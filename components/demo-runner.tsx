"use client";

interface DemoRunnerProps {
  onProgress: (message: string) => void;
}

export default function DemoRunner({ onProgress }: DemoRunnerProps) {
  // This component is now integrated into the main page
  // Keeping it as a placeholder for potential future use
  return null;
}